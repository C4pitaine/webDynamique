<div class="alertDeleteUser">
    <div class="containerDeleteUser">
        <h4>Êtes vous sûr de vouloir supprimer : {{title}} {{type}}</h4>
        <div class="deleteChoice">
            <a href="{{path(route,{'id':id})}}" class="btn btn-danger me-2 mt-2">Supprimer</a>
            <div class="btn btn-primary annulerDeleteUser mt-2">Annuler</div>
        </div>
    </div>
</div>