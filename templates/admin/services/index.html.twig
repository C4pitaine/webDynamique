{% extends 'admin/base.html.twig' %}

{% block title %}Services{% endblock %}


{% block body %}
    <div class="container-fluid full">
        <h1 class="my-5">Gestion des services</h1>
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Title</th>
                    <th>Description</th>
                    <th>Logo</th>
                    <th class="text-center">Action</th>
                </tr>
            </thead>
            <tbody>
                {% for service in pagination.data %}
                    <tr>
                        <td>{{service.id}}</td>
                        <td>{{service.title}}</td>
                        <td>{{service.logo}}</td>
                        <td class="text-center"> 
                            <a href="{{path('admin_ads_edit', {'id':ad.id})}}" class="btn btn-warning"><i class="fas fa-edit"></i></a>
                            <a href="{{path('admin_ads_delete', {'id':ad.id})}}" class="btn btn-danger"><i class="fas fa-trash"></i></a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
        {{pagination.display}}
    </div>
{% endblock %}
