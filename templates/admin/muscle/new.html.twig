{% extends "admin/base.html.twig" %}

{% block title %}Ajout Muscle{% endblock %}

{% block body %}
    <div class="slideAdmin">
        <div class="wrapper">
            <div class="form">
                <div class="titleBack">
                    <h2>Ajout d'un muscle</h2>
                    <a href="{{path('admin_muscle_index')}}" class="btn btn-secondary">Retour</a>
                </div>
                {{form_start(formMuscle)}}
                    {{form_widget(formMuscle)}}
                    <button type="submit" class="btn btn-primary my-3">Ajouter</button>
                {{form_end(formMuscle)}}
            </div>
        </div>
    </div>
{% endblock %}